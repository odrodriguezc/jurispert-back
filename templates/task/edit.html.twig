{% extends 'base.html.twig' %}

{% block title %}Edit Task
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-5 mb-3 text-center">Modifier une tache</h1>

		{{ include('task/_form.html.twig', {'button_label': 'Enregistrer'}) }}

		{{ include('task/_delete_form.html.twig') }}
		<a class="btn btn-outline-info" href="{{ path('task_index') }}">Liste de taches</a>
	</div>
{% endblock %}
