{% extends 'base.html.twig' %}

{% block title %}Edit Customer
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mb-3 mt-5 text-center">Edit Customer</h1>

		{{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('customer_index') }}">Liste de clients</a>

		{{ include('customer/_delete_form.html.twig') }}
	</div>
{% endblock %}
