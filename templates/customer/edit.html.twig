{% extends 'base.html.twig' %}

{% block title %}modifier client
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mb-3 mt-5 text-center">Modifier
		</h1>

		{{ include('customer/_form.html.twig', {'button_label': 'Enregistrer'}) }}

		<hr>
		<div class="mb-3">
			{{ include('customer/_delete_form.html.twig') }}
			<a class="btn btn-outline-info mr-2 mt-3" href="{{ path('customer_index') }}">Liste de clients</a>

		</div>
	</div>
{% endblock %}
